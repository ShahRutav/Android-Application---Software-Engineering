<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weddingplanner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BlogDetailsAdminActivity"></activity>
        <activity android:name=".ProductDetailsAdminActivity" />
        <activity android:name=".BlogDetailsActivity" />
        <activity android:name=".CategoryProducts" />
        <activity android:name=".AdminBlogActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AdminNewProductActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>